message(STATUS "        ...maneuveuring")

set(SRC_FILES
        FrManoeuvringDamping.cpp
        )

set(FRYDOM_HYDRODYNAMICS_MANEUVEURING_SOURCES "")
foreach(file ${SRC_FILES})
    list(APPEND FRYDOM_HYDRODYNAMICS_MANEUVEURING_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/${file})
endforeach()

set(FRYDOM_HYDRODYNAMICS_MANEUVEURING_SOURCES ${FRYDOM_HYDRODYNAMICS_MANEUVEURING_SOURCES} PARENT_SCOPE)
