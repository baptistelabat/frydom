message(STATUS "    ...seakeeping")

add_subdirectory(linear)

set(FRYDOM_SEAKEEPING_SOURCES "")

list(APPEND FRYDOM_SEAKEEPING_SOURCES ${FRYDOM_HYDRODYNAMICS_LINEAR_SOURCES})

set(FRYDOM_SEAKEEPING_SOURCES ${FRYDOM_SEAKEEPING_SOURCES} PARENT_SCOPE)