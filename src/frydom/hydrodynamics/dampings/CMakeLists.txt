message(STATUS "        ...dampings")

set(SRC_FILES
        FrLinearDamping.cpp
        FrQuadraticDamping.cpp
        )

set(FRYDOM_HYDRODYNAMICS_DAMPINGS_SOURCES "")
foreach(file ${SRC_FILES})
    list(APPEND FRYDOM_HYDRODYNAMICS_DAMPINGS_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/${file})
endforeach()

set(FRYDOM_HYDRODYNAMICS_DAMPINGS_SOURCES ${FRYDOM_HYDRODYNAMICS_DAMPINGS_SOURCES} PARENT_SCOPE)
